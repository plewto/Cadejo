(println "--> Loading cadejo.core")
 (ns cadejo.core
  ;; (:require [seesaw.core])
  ;; (:require [cadejo.config])
  ;; (:use [overtone.core])
  ;; (:require [cadejo.midi.scene])
  ;; (:require [cadejo.midi.channel])
  ;; (:require [cadejo.midi.performance])
  ;; (:require [cadejo.midi.program-bank])
  ;; (:require [cadejo.midi.pitch-bend])
  ;; (:require [cadejo.midi.pressure])
  ;; (:require [cadejo.midi.cc])
  ;; (:require [cadejo.midi.mono-mode])
  ;; (:require [cadejo.midi.poly-mode])
  ;; ;(:require [cadejo.demo])
   (:require [cadejo.demo2])


  ;; TESTING
  ;(:require [cadejo.ui.indicator.lamp])
  ;(:require [cadejo.ui.midi.bank-editor])
   ;(:require [cadejo.test-combo])
   ;(:require [cadejo.ui.util.laf])
   
   )
            
  
;(cadejo.ui.util.laf/show-lnf-frame)

